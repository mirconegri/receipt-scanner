/**
 * Update APP_VERSION when you bump the app's version — it's shown in
 * Settings > About.
 */
export const REPO_URL = 'https://github.com/mirconegri/receipt-scanner';

export const APP_VERSION = '0.1.0';
